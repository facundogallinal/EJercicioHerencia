namespace Library
{
    public class CommonDriver : Driver
    {
        public CommonDriver(string name, string lastName, int Id, int qualification, string vehicle, string bio) : base(name, lastName, Id, qualification, vehicle, bio)
        {
        }
    }
}